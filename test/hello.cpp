#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define all(x) x.begin(),x.end() //is_sorted(all(x))
#define pb push_back(x)
#define yes cout<<"YES"<<endl;
#define no cout<<"NO"<<endl;
#define gcd(a, b) __gcd(a, b)
#define lcm(a, b) (a / gcd(a, b)) * b

int main(){
  int t; 
  cin>>t;
  while(t--)
  {
 
  }
  return 0;
}